# Week 5 Day 2

## 지문

1. Promise 기반의 `readFilePromise(path)` 함수를 작성하세요.  
   - `new Promise`와 `setTimeout` 사용 (500ms 지연).  
   - 성공 시 문자열 반환, 실패 시 `reject` 호출.  
2. `readFilePromise('file.txt').then(...).catch(...)` 로 결과와 에러를 console에 출력하세요.
