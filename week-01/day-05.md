# Week 1 Day 5

## 지문

1. 빈 배열 `fruits`를 만들고, `.push()`로 `"🍎"`, `"🍌"`, `"🍇"`을 추가하세요.  
2. `.map()`으로 각 과일 뒤에 `" 좋아요"`를 붙인 새 배열을 만들고 콘솔에 출력하세요.  
3. `.filter()`로 `"🍌"`만 남긴 배열을 출력하세요.  
4. `.pop()`으로 마지막 요소를 제거한 후 최종 배열을 출력하세요.
